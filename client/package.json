{
  "name": "navimap-charts-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Viewer NaviMap Charts — non-ECDIS, not for navigation",
  "scripts": {
    "dev": "vite",
    "live": "node server.mjs",
    "build": "vite build",
    "start": "node server.mjs --production",
    "preview": "vite preview",
    "demo:live": "node scripts/demo-live.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "maplibre-gl": "^6.9.1"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
